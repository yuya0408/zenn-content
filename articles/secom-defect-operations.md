---
title: "機械学習モデルは静かに古びる ― 半導体不良予測のドリフト監視と再学習の線引き"
emoji: "📡"
type: "tech"
topics: ["python", "mlops", "機械学習", "製造業", "監視"]
published: true
---

これは、半導体製造データ SECOM を題材にした不良予測プロジェクトの記録の3本目、「運用編」である。同じ1つのプロジェクトを、3つの角度から書いている。

- **考察編**（[記事](https://zenn.dev/yuya0408/articles/secom-defect-analysis)）― このデータが何を物語っているかを読み解く話
- **実装編**（[記事](https://zenn.dev/yuya0408/articles/secom-defect-serving)）― 分析結果を API・コンテナとして動かせる形にする話
- **運用編（本記事）**― 動かし続けるために何を監視し、異常を見つけたらどう動くかの話

3本は同じプロジェクトの別の顔だが、本記事は単独でも追えるように、前提となる発見は本文中で簡単に補っている。

実装編では、「精度に限界のあるモデルでも、動かせる形にして初めて現場で使う土俵に乗る」という趣旨で記事を書いた。本記事が扱うのはその先の「土俵に乗せたモデルを、どう見守り続けるか」である。

:::message
私は元・電子部品メーカーの製造技術職で、現在はIT業界に身を移している。MLOpsの専門家ではなく、ここで扱うドリフト監視の知見も豊富ではない。本記事は「精度の高くないモデルを現場に置き続けるなら何が要るか」を、公開データの上で手を動かして確かめた記録であって、運用のベストプラクティス集ではない。
:::

---

## 1. なぜ「運用編」が必要だったか

考察編で、SECOM の最大の発見として挙げたのが「品種ミックス」だった。センサーの欠損パターンでサンプルを分類すると、不良率がまるで違うグループに分かれる――同じ製造ラインに複数の品種が入れ替わり流れていることを示す構造だ。そして分析を進めるなかで、見逃せない事実にぶつかった。

**時系列で評価すると、テスト期に「学習時には存在しなかった品種グループ」が次々と現れる。** あるfoldではテストデータの35%が新グループだった。

これは分析上は「品種情報をモデルに組み込めなかった理由」のひとつだったが、運用の視点で読み直すと、もっと深い意味を持つ。**学習したときの世界と、いま流れてくる製品の世界は、時間とともにズレていく**――いわゆるデータドリフトだ。実装編で推論APIにしたこのモデルを、そのまま現場に置き続ければ、いつかモデルは「知らない世界」を相手に予測を行うことになる。

ただでさえ精度が0.6で余裕がないのに、土台の前提が崩れたら予測は意味を失う。だから運用編のテーマはこうなる――**モデルが「知らない世界」を相手にしていないかを、どう見張るか**。

---

## 2. 何を監視するのか：3層に分けて考える

ドリフトと一口に言っても、見るべきものは1つではない。本プロジェクトでは3層に分けた。

| 層 | 見るもの | ラベルの要否 |
| --- | --- | --- |
| 入力ドリフト | 入ってくるデータの分布が学習時とズレていないか | 不要（即時に分かる） |
| 予測ドリフト | モデルが出す不良確率の分布が変わっていないか | 不要（即時に分かる） |
| 性能ドリフト | 実際の検査結果に対して、予測が当たっているか | **必要（検査結果が出てから）** |

製造現場で重要なのは、**検査結果（真のラベル）は遅れて確定する**という点だ。製品を作った瞬間に「不良だったか」は分からない。検査を経て初めて分かる。だから性能ドリフト（当たっているか）は事後にしか測れない。

これによって監視の構え方が決まる。**入力・予測ドリフトで「即時に異常の気配」を察知し、検査結果が溜まったら性能で裏を取る**、という二段構えだ。

---

## 3. 品種ミックスを、そのまま監視指標に転用する

入力ドリフトをどう測るか。ここで考察編の発見がそのまま効いた。

欠損率10〜70%の列の欠損パターンで「品種グループ」を定義する――欠損のしかたが、その製品がどの工程を通った（あるいはバイパスした）かの手がかりになる、という見立てだ。学習（参照）期に現れたグループの集合を覚えておき、新しく入ってきたデータに対して、学習時に存在しなかったグループの割合（未知グループ率）を計算する。

```python
# src/monitoring.py より（要点）
def missing_pattern_signature(df, flag_cols):
    """欠損パターンからサンプルごとのグループ署名（"010..." 形式）を作る。"""
    miss = df[flag_cols].isna().astype(int)
    return miss.apply(lambda row: "".join(map(str, row.values)), axis=1)

def unknown_group_rate(reference_signatures, new_signatures):
    """学習時に存在しなかったグループ署名を持つ新データの割合。"""
    known = set(reference_signatures)
    return sum(1 for s in new_signatures if s not in known) / len(new_signatures)
```

予測分布のズレには **PSI（Population Stability Index）** を使った。学習時のスコア分布を分位点でビンに切り、新データの各ビン比率と比べる定番の指標で、慣用的な目安は「0.1未満なら安定、0.1〜0.25は要注意、0.25超は大きな変化」である。

外部のドリフト監視ツール（Evidently など）を使う選択肢もあったが、今回は依存を増やさず、素の numpy/pandas で書いた。中身がブラックボックスにならず、何を計算しているかを自分で説明できる状態にしておきたかったからだ。

---

## 4. 「動かして見せる」：時系列リプレイ

監視ロジックを書いても、流れてくるデータがなければ意味がない。しかし SECOM は2008年で完結した凍結データで、本番ラインがあるわけではない。

そこで、**凍結データの上でドリフトを再現する**ことにした。検査タイムスタンプで全データを月ごとに分け、最初の数か月を「参照（学習）期」、以降の月を「本番運用月」として順にモデルへ流す。リプレイ手法である。

```bash
python -m scripts.replay_drift
# => reports/drift_report.md と図を生成
```

本プロジェクトのデータは2008年7月〜10月の4か月。**7〜8月を参照期としてモデルを学習し、9月・10月を本番月として流した**。

凍結データには、本番ラインにはない強みがひとつある。**全期間の検査結果（ラベル）が揃っている**ことだ。本番では遅れて確定する性能ドリフトを、ここでは即座に測れる。デモのための割り切りだが、おかげで3層すべてを通しで実演できる。

結果がこれだ。

![時系列リプレイによるドリフト指標（月別）](/images/secom-defect-operations/fig7_drift_replay.png)

| 月 | 未知グループ率 | 予測PSI | PR-AUC | 判定 |
| --- | --- | --- | --- | --- |
| 2008-09 | 9.2% | 0.279 | 0.202 | RED |
| 2008-10 | **35.9%** | 0.397 | 0.141 | RED |

未知グループ率が **9.2% → 35.9%** と月を追って増えている。冒頭で触れた「テスト期に新グループが現れる」現象が、運用指標として目の前で動いた瞬間だった。10月にはPR-AUCも参照期の基準（0.18）を下回り、性能の劣化まで観測できた。

正直に言えば、両月とも「赤」で、緑や黄の穏やかな段階を経ずにいきなり危険水準に入っている。これは取り繕わずに書いておきたい。**9月の時点で、モデルが学習した7〜8月の世界とは、もうかなりズレていた**ということだ。実際このデータは、9月に不良率が3%を切るほど大きく変動しており、その符合とも読める。

---

## 5. レビューで拾った罠：基準値が「満点」になっていた

監視を組む過程で、計算した基準 PR-AUC が **1.0000**、予測PSIが **3.8〜4.0** という、明らかにおかしな値が出た。**1.0 という「満点」はあり得ない**――過学習かリークを疑うべき赤信号だ。

異常の出どころを追うと、基準性能を求めるコードが、参照期のスコアをこう計算していた。

```python
model.fit(X_ref, y_ref)                       # 参照データで学習
ref_scores = model.predict_proba(X_ref)[:, 1] # ← 同じ参照データを採点
baseline_pr_auc = average_precision_score(y_ref, ref_scores)
```

原因は、**学習に使ったのと同じデータをそのまま採点していた**こと。Random Forest は学習データをほぼ暗記するので、訓練行への予測は不良≈1.0・合格≈0.0 にきれいに張り付く。その結果、

- 暗記したデータ上では完璧に分類できるので、基準 PR-AUC が 1.0 になる。当然その後の全月が「基準割れ」になり、性能の判定が無意味化する。
- 暗記スコアは0と1に張り付いた極端な分布になる。一方で本番月のスコアは0.1前後に穏やかに広がる。この2つを比べれば「激しく違う」のは当たり前で、PSIが大きく変動する。だがその違いは過学習が生んだ見せかけで、実ドリフトではない。

これを直す方法については複数の選択肢がある。まず単純に思い浮かぶのは「参照期を train/valid に分ける（ホールドアウト）方法」だ。だが参照期はデータが618件で、そのうち陽性はわずか65件と少なく、1回の分割では検証側の陽性が十数件しか残らない。それでは基準値が「どの陽性がたまたま検証側に入ったか」で運任せの結果になってしまう。

そこで、分割を5回ローテーションして全件を一度ずつ検証に回す、**交差検証の out-of-fold 予測**を使った。各サンプルを「それを学習に使っていないモデル」で採点する仕組みで、ホールドアウトと比較して全データを無駄なく使うことができる。

```python
ref_scores = cross_val_predict(
    make_model(), X_ref, y_ref,
    cv=StratifiedKFold(5, shuffle=True, random_state=42),
    method="predict_proba",
)[:, 1]
baseline_pr_auc = average_precision_score(y_ref, ref_scores)
```

これで基準 PR-AUC は **0.1825** に落ち着いた。考察編で厳密に評価したときの値 0.1346 と同じく、「ランダム水準の約2倍」程度の低い値で、桁が合う。基準が現実的になり、PSI も意味のある値に戻った。

「自分の答案を自分で採点すれば満点が出る」――言われれば当たり前だが、出力された数字を鵜呑みにすると見落とす。大事なのは、**「1.0 はおかしい」と立ち止まれるか**だ。考察編でも、特徴量選択を交差検証の外でやってしまうリークに気づいて測り直したが、それと根は同じで、**評価の独立性を一箇所でも崩すと、数字は簡単に嘘をつく**。出てきた異常に気づいて潰せるかどうかが、コードを本番に乗せる前の最後の砦になる。

---

## 6. 検知したら、どう動くか

監視の本番は「異常を見つけたあと」だ。本プロジェクトでは、深刻度を緑/黄/赤の3段階で判定し、段階ごとに動きを変える設計にした。判定はコードで行うが、**実際のアクションは人間が承認する**ことを前提にしている。

```python
# src/monitoring.py より
RECOMMENDED_ACTIONS = {
    "green":  "通常運用。指標を記録するのみ。",
    "yellow": "推論は継続しつつ通知・ダッシュボード警告。レスポンスに drift_warning を付与。",
    "red":    "推論は返すが信頼度を下げ、該当ロットを全数検査へ。再学習を起票（自動実行しない）。",
}
```

設計で意識したのは3点だ。

**赤でもAPIを止めない。** 製造ラインの検査補助は、止まると現場が困る。だから赤でも予測は返す。ただし「これは信用しすぎるな」という信号（`drift_warning`）を一緒に出し、該当ロットは自動振り分けに使わず全数検査へ回す。フェイルセーフは「止める」ではなく「人の目を増やす」側に倒した。

**自動で再学習・自動でデプロイしない。** 赤になったら再学習を「起票」するが、自動では走らせない。新しいモデルは必ず時系列ホールドアウトで評価し、現行と比べて良いか同等のときだけ昇格させる。悪化していたらロールバックする。

**再学習は目的ではなく手段。** ここが製造現場上がりとして一番こだわった点だ。考察編では、せっかく見つけた品種情報をモデルに足しても精度は上がらなかった。これを踏まえると、このデータでは再学習しても精度が上がるとは限らない。だから監視の主目的を「精度を上げること」に置くのは筋が悪い。置くべきは、**入力が学習時から乖離してモデルが無意味化していないかの検知と、しきい値の再調整・再学習を起票するかの判断**だ。

---

## 7. これは「実演」であって「本番監視」ではない

最後に、誇張を避けるために線を引いておきたい。

本記事でやったのは、**凍結データの上でドリフト監視機構を動かして見せた**ことだ。本番の製造ライン監視とは、決定的に違う点がいくつもある。

- 本番では検査ラベルが遅れて確定する。ここでは全期間揃っているので性能ドリフトを即座に測れたが、これは凍結データだからこその割り切りだ。
- 本番ではアラート・ダッシュボード・再学習パイプラインといった常時稼働の基盤が要る。本記事は判定ロジックと実演までで、その基盤までは作っていない。「設計はこう」という思想をコードと文章で示すに留めた。
- 仮にこのモデルを Hugging Face Spaces などに載せてデモ公開しても、そこに流れるのはデモ利用者のトラフィックであって、製造ラインの実分布ではない。

それでも、**監視機構が確かに動き、考察編で見つけたドリフトを運用の指標として捉えられる**ことは示せた。精度0.6のモデルを現場に置き続けるなら、「精度を上げる」より先に「壊れていないかを見張る」仕組みが要る。当実装でその入口は作れたと思う。

---

## 結び

3本を通して言いたかったのは、ひとつのことに尽きる。**ノートブックでスコアを出すことと、モデルを現場に置き続けることの間には、いくつもの壁がある**、ということだ。

考察編では「データを読む」壁を、実装編では「動かせる形にする」壁を、そして運用編では「動かし続ける」壁を、それぞれ手で確かめた。どれも派手な技術ではないが、現場データを元にモデルを作成し、運用に乗せるまでは確実に必要な作業だ。

精度0.6のモデルは、それ単体では頼りない。だが、何を見張ればいいかを決め、異常を見つけたときの動き方を用意しておけば、現場に「置き続ける」ことはできる。製造業にいた頃に学んだ「変化点を見張る」という感覚は、モデルの運用にもそのまま効いた。データを読む力、動かす技術、そして見守る仕組み。その3つが揃って、ようやく分析は現場に根づくのだと思う。

コードは[GitHub](https://github.com/yuya0408/secom-defect-prediction)に公開している。考察編・実装編とあわせて、興味があれば覗いてみてほしい。
